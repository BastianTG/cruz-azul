version: '3.8'

services:
  frontend:
    build: ./frontend
    container_name: cruzazul-frontend
    ports:
      - "80:3000"
    environment:
      - NODE_ENV=production
      - DB_HOST=18.235.20.153
      - DB_PORT=5432
      - DB_NAME=cruzazul
      - DB_USER=postgres
      - DB_PASSWORD=tu_password
